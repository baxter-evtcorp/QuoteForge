apiVersion: v1
kind: Service
metadata:
  name: quoteforge
  namespace: quoteforge
spec:
  type: NodePort
  selector:
    app: quoteforge
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30500
